# NMap

NMap is a **network scanner** that’s used to identify services running on remote systems.
It sends network traffic to:

  - Determine if something is listening on a port
  - If configured, identify the services listening on the port by grabbing any response data.
